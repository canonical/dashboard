name: Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  app-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run tests
        run: |
          cd dashboard
          make test
      - name: Check static files
        run: |
          cd dashboard
          make lint
